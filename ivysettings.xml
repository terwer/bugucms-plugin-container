<ivysettings>
    <settings defaultResolver="chained"/>
    <resolvers>
        <chain name="chained" returnFirst="true">
            <filesystem name="libraries">
                <artifact pattern="${ivy.conf.dir}/repository/[module]-[revision].[ext]"/>
            </filesystem>
            <url name="aliyun-mirror">
                <artifact pattern="http://maven.aliyun.com/nexus/content/repositories/central/[organisation]/[module]/[revision]/[module]-[revision].[ext]"/>
            </url>
        </chain>
    </resolvers>
</ivysettings>