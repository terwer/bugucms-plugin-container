# bugucms

Lightweight Pluguable Development Plantform

# How to run

```
cd $WORKSPACE
cd src/main/webapp
npm get registry
npm install
npm run build

cd $WORKSPACE
mvnw clean package
mvnw spring-boot:run
```