<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bgapp">
    <!-- 校验用户名 -->
    <select id="vali_by_account" resultType="int">
		select count(1) from bg_users where mobile = #{account}
	</select>
    <!-- 校验密码 -->
    <select id="vali_by_password" resultType="int">
		select count(1) from bg_users where mobile = #{account} and password = md5(#{password})
	</select>
    <!-- 获取用户信息 -->
    <select id="get_user_by_account" resultType="com.terwergreen.middle.user.dto.UserDTO">
        select user_name as accoutId,nick_name as nickName, user_profile as userProfile,mobile from bg_users where mobile = #{account}
    </select>
</mapper>